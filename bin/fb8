#!/usr/bin/env ruby
# frozen_string_literal: true

require 'fabric8'

begin
  cli = Fabric8::Cli.new(ARGV)
  cli.tasks.each { |task| cli.run_build(task) }
rescue => e
  Fabric8::Output.out :error, "#{e.message}\n#{e.backtrace.join("\n")}"
end
