#!/usr/bin/env ruby
# frozen_string_literal: true

require 'commander/import'
require 'fabric8'

program :name, 'Fabric8'
program :version, '1.0.0'
program :description, 'A universal interface layer for compilers and tools'

tools = [Fabric8::Tool.new('.\tools\test.rb')]

tools.each do |tool|
  command tool.name do |c|

  end
end
